component: common-utils
product: MindXDL
systemEnv:
  - workspace
  - processor
version:
test:
  input:
    files:
  process:
    type: ut
    testTool: shell
    execdir: "{{systemEnv.workspace}}/{{component}}/build"
    parameters: ""
    script: "test.sh"
  output: